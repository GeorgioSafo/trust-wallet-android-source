ext {
    // region android versions
    COMPILE_SDK_VERSION = 27
    TARGET_SDK_VERSION = 27
    MIN_SDK_VERSION = 23
    // endregion

    //region version
    VERSION_MAJOR = 1
    VERSION_MINOR = 0
    VERSION_HOTFIX = 0
    // endregion
}